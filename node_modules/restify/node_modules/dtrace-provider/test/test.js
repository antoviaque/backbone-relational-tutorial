function sleep() {
    var now = new Date().getTime();
    while(new Date().getTime() < now + 60000) {
        // do nothing
    }
}
var d = require('../dtrace-provider');
var dtp = d.createDTraceProvider("testlibusdt");
var p1 = dtp.addProbe("myprobe", "int");
dtp.enable();
p1.fire(function(p) { return [1]; });
